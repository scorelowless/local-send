This folder should contain Android-specific native code for the platform bridge (e.g., Wi-Fi Direct handlers, share intent receivers).

Implementations should use MethodChannel or Pigeon and be registered from Dart via DI.
